<main class="registration-page">
    <section class="registration-artwork" aria-label="Registrasi vendor">
        <article class="registration-card @(Wide ? "registration-card-wide" : null) @CardClass">
            @ChildContent
        </article>
    </section>
</main>
